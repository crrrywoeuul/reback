apiVersion: v1
kind: Service
metadata:
  name: lovely
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "lovely"
    port: 8080
  selector:
    app: lovely
