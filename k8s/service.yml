apiVersion: v1
kind: Service
metadata:
  name: skill
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "skill"
    port: 8080
  selector:
    app: skill
