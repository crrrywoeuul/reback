apiVersion: v1
kind: Service
metadata:
  name: spare
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "spare"
    port: 8080
  selector:
    app: spare
